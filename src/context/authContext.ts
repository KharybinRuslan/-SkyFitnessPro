/** Re-export for backwards compatibility (cache may still request authContext.ts) */
export {
  AuthContext,
  type AuthContextValue,
  type AuthState,
  type RegisterOutcome,
} from "./authContextState";
